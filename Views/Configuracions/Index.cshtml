@model IEnumerable<EntreEspeciesNuevo.Models.Configuracion>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdPermisoNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdRolNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdPermisoNavigation.IdPermiso)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdRolNavigation.IdRol)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdConfiguracion">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdConfiguracion">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdConfiguracion">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
